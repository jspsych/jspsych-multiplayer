---
"@jspsych-multiplayer/utils": minor
---

Add `@jspsych-multiplayer/utils`, the helpers the multiplayer plugins and adapters share: `isMultiplayerError`, `outcomeOf` and the `MultiplayerOutcome` type, `pluginTimeout`, `remainingParticipants`, `sealedGroupSize`, `withoutLeft`, the `waitForAll` barrier, and the adapter helpers `sessionIdFromUrl`, `tabId`, `generateId`, and `validateId`.
