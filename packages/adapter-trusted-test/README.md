# @jspsych-multiplayer/adapter-trusted-test

⚠️ **Throwaway package.** This exists only to validate npm trusted publishing (OIDC) for the
`@jspsych-multiplayer` scope. It has no real functionality and should be deprecated/unpublished
once the publishing flow is confirmed working.
