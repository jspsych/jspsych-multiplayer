{
  "extends": "@jspsych/config/tsconfig.contrib.json",
  "compilerOptions": {
    "baseUrl": ".",
    "resolveJsonModule": true,
    "paths": {
      // Resolve the shared helpers to their source, so tests and type checks don't need a build
      "@jspsych-multiplayer/utils": ["../utils/src"]
    }
  },
  "include": ["src"]
}
